using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Teleporter : MonoBehaviour {

    public Animator faderAC;


    //public void TeleportTo(GameObject player, GameObject newLocation)
    //{
    //    faderAC.SetBool("fade", true);
    //    // wait for some time
    //    player.transform.position = newLocation.transform.position;
    //    player.transform.LookAt(newLocation.transform);
    //    faderAC.SetBool("fade", false);
    //}

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
